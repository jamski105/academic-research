httpx>=0.28.1
pypdf>=4.0.0
pyyaml>=6.0.3
# Pflicht-Dependency des Excel-Backends document-skills:xlsx, das /excel und
# /pickup aufrufen — Issue #367, Backend extern seit #445.
openpyxl>=3.1.5
# Renderer von word-export/slide-export (Issue #446): skills/word-export/scripts/
# render_docx.py und skills/slide-export/scripts/render_pptx.py erzeugen die
# .docx/.pptx deterministisch -- ohne diese Pakete enden /academic-research:word
# und :slides ohne Zieldatei. Zweitstelle: pyproject.toml [project.dependencies].
python-docx>=1.1
python-pptx>=1.0
# requests>=2.32.4: CVE-2024-35195 (cert verification bypass), CVE-2024-47081 (.netrc credential leak)
requests>=2.32.4
# lxml>=5.2.0: CVE-2022-2309 (NULL pointer deref)
lxml>=5.2.0
# Barcode-Generierung fuer /academic-research:pickup (Skill: barcode_utils)
python-barcode[images]>=0.16.1
# academic-vault MCP-Server (Vault): FastMCP-SDK + Vektor-Suche-Extension
# <2: mcp 2.0.0 (PyPI) entfernt mcp.server.fastmcp, das academic_vault/server.py
# importiert — der Server startet sonst nur noch mit "mcp SDK nicht installiert".
mcp>=1.0,<3
sqlite-vec>=0.1.0
# strikte csl_json-Validierung in vault.add_paper (Issue #213)
jsonschema>=4.0
# optional — nur fuer Zotero-Import (Skill: zotero-import)
pyzotero>=1.5
# lokale Embedding-Pipeline fuer die Vektor-Suche (Issue #372).
# Befuellt chunk_embeddings beim Ingest und liefert vault.search(rerank=True)
# echte Vektortreffer statt reiner FTS5-Umsortierung
# (intfloat/multilingual-e5-small, 384d, MIT).
# Zieht Torch nach; auf Linux installiert pip standardmaessig das CUDA-Wheel.
# Wer das CPU-Wheel will (deutlich kleiner, fuer diesen Vault ausreichend),
# installiert Torch vorher separat:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# >=5.0: requirement for CrossEncoder(cache_folder=...) in #714.
sentence-transformers>=5.0
# HINWEIS (Issue #376/#714): bewusst KEIN `transformers<5.0`-Cap hier. Der
# lokale Reranker-Fallback laeuft seit #714 ueber
# `sentence_transformers.CrossEncoder` mit dem hier gepinnten `transformers`
# -- kein Versionskonflikt mehr, kein manueller Installationsschritt.
# Fuzzy-Snap in academic_vault/verbatim.py (Issue #511): findet die
# naechstliegende Textstelle fuer leicht abweichende Zitat-Kandidaten.
# Zweitstelle: pyproject.toml [project.dependencies].
rapidfuzz>=3.14.5
# Rechenkern von quantitative-analysis (Issue #610):
# skills/quantitative-analysis/scripts/analyze.py rechnet Teststatistiken,
# Effektstaerken und Bootstrap-Intervalle. numpy/scipy kommen ohnehin mit
# sentence-transformers ins Environment, stehen hier aber explizit -- sonst
# haengt ein Skill an einer transitiven Dependency, die niemand versprochen hat.
# Zweitstelle: pyproject.toml [project.dependencies].
numpy>=1.26
scipy>=1.11
# Strukturerhaltende Tabellenextraktion (Issue #630), academic_vault/tables.py.
# Seit #723 Pflicht-Dependency statt auskommentiertes Extra: reines Python,
# kein Versionskonflikt mit dem Bestand. Faellt das Paket in einer realen
# Installation dennoch weg, laeuft der Volltextpfad unveraendert weiter und
# vault.extract_tables() meldet status="backend-missing" samt
# Installationsanweisung statt einer Exception.
# Zweitstelle: pyproject.toml [project.dependencies].
pdfplumber>=0.11
