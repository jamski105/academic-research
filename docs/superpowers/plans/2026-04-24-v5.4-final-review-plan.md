# v5.4 — Final Review Fixes + Cookbook Alignment — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 29 Findings aus Tri-Agent-Review in einem Release v5.4.0 fixen — Konventions-Alignment zu `anthropics/skills`, Struktur-Konsistenz über 13 Skills, Repo-Cleanup.

**Architecture:** 9 Git-Worktrees in 2 parallelen Waves + 1 sequenzielle Wave. Konflikt-Vermeidung durch Datei-Domäne (Wave 1) und Skill-Name-Sharding (Wave 2). Cleanup in Wave 3 erst nach allem anderen.

**Tech Stack:** Python 3.11+, pytest 9.x, Git-Worktrees, YAML-Frontmatter, Markdown

**Referenz-Spec:** `docs/superpowers/specs/2026-04-24-v5.4-final-review-design.md`

---

## Phase 0: Pre-Flight Setup

### Task 0: Branch + Worktrees + Baseline

**Files:**
- Modify: `/Users/j65674/Repos/academic-research` (branch state)
- Create: 8 Worktrees unter `../academic-research-w1-a/`, `-w1-b/`, `-w1-f/`, `-w1-g/`, `-w2-s1/`, `-w2-s2/`, `-w2-s3/`, `-w2-s4/`, `-w3-h/`

- [ ] **Step 1: Feature-Branch anlegen**

```bash
cd /Users/j65674/Repos/academic-research
git checkout -b v5.4-final-review
git push -u origin v5.4-final-review
```

- [ ] **Step 2: Baseline-Grep für H1-Cross-Refs dokumentieren**

```bash
grep -rn -E "(Abstract Generator|Academic Context|Chapter Writer|Citation Extraction|Literature Gap Analysis|Source Quality Audit|Research Question Refiner|Submission Checker|Style Evaluator|Title Generator|Methodology Advisor|Plagiarism Check|Advisor(?!\")|Academic Research)" skills/ agents/ commands/ README.md 2>/dev/null | grep -v ".git" | tee /tmp/v5.4-baseline-titlecase-refs.txt
wc -l /tmp/v5.4-baseline-titlecase-refs.txt
```

Expected: >= 20 Treffer dokumentiert (Baseline für Wave 2 Verification).

- [ ] **Step 3: Baseline-Grep für ./-Prefix-Inkonsistenz dokumentieren**

```bash
echo "=== OHNE ./-Prefix ==="
grep -rn -E "\`(academic_context|literature_state|writing_state)\.md\`" skills/ 2>/dev/null | grep -v "\./" | tee /tmp/v5.4-baseline-no-prefix.txt
echo "=== MIT ./-Prefix ==="
grep -rn -E "\`\./(academic_context|literature_state|writing_state)\.md\`" skills/ 2>/dev/null | tee /tmp/v5.4-baseline-with-prefix.txt
echo "Ohne: $(wc -l < /tmp/v5.4-baseline-no-prefix.txt); Mit: $(wc -l < /tmp/v5.4-baseline-with-prefix.txt)"
```

Expected: ~61 ohne, ~14 mit.

- [ ] **Step 4: 8 Worktrees für Wave 1 + 2 anlegen**

```bash
cd /Users/j65674/Repos/academic-research
for tree in w1-a w1-b w1-f w1-g w2-s1 w2-s2 w2-s3 w2-s4; do
  git worktree add "../academic-research-${tree}" -b "v5.4-${tree}" v5.4-final-review
done
git worktree list
```

Expected: 9 Einträge (main + 8 Worktrees).

- [ ] **Step 5: Commit Baseline-Dokumentation ins Feature-Branch**

```bash
cat /tmp/v5.4-baseline-titlecase-refs.txt /tmp/v5.4-baseline-no-prefix.txt > docs/superpowers/plans/v5.4-baselines.txt
git add docs/superpowers/plans/v5.4-baselines.txt
git commit -m "chore(v5.4): Baseline-Grep für H1 Cross-Refs und ./-Prefix"
```

---

## Wave 1: 4 Trees parallel

Jeder Tree ist ein eigener Subagent-Dispatch mit `cwd` auf den jeweiligen Worktree-Pfad.

### Task W1.A: Structural Hygiene

**Worktree:** `../academic-research-w1-a/` (Branch `v5.4-w1-a`)

**Files:**
- Delete: `settings.json`, `.mcp.json`
- Modify: `.gitignore`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`

- [ ] **Step 1: Root-`settings.json` löschen (H2)**

```bash
cd ../academic-research-w1-a
git rm settings.json
```

- [ ] **Step 2: `.mcp.json` löschen (H3)**

```bash
git rm .mcp.json
```

- [ ] **Step 3: `.pytest_cache/` aus Tracking + `.gitignore` erweitern (L1)**

```bash
git rm --cached -r .pytest_cache 2>/dev/null || true
```

Edit `.gitignore`: Zwischen `__pycache__/` und `*.py[cod]` die Zeile `.pytest_cache/` einfügen. Finale .gitignore-Struktur:

```
# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
venv/

# Plugin local config (user-specific)
*.local.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local settings (user-specific, not part of plugin)
.claude/

# Runtime data (stored in ~/.academic-research/)
sessions/
pdfs/

# Debug logs
firebase-debug.log

# Secrets
.env
.env.local
```

- [ ] **Step 4: `owner.email` in beiden Manifesten ergänzen (L2)**

In `.claude-plugin/plugin.json` das `author`-Objekt erweitern:

```json
"author": {
  "name": "Jonas Ahler",
  "email": "jam@ahler.org",
  "url": "https://github.com/jamski105"
}
```

Dasselbe in `.claude-plugin/marketplace.json` unter `owner`.

- [ ] **Step 5: MCP-Referenz aus `plugin.json` description entfernen (H3)**

In `.claude-plugin/plugin.json`, im `description`-Feld streichen sofern vorhanden ("MCP", "Model Context Protocol"). Aktuell ist die description:

> "Modulares akademisches Forschungs-Toolkit mit 13 selbstaktivierenden Skills, 5D-Scoring, Excel-Export und KI-Stil-Erkennung. Durchsucht Semantic Scholar, CrossRef, OpenAlex, BASE, Google Scholar und weitere Quellen."

Keine MCP-Erwähnung vorhanden — unverändert lassen. Nur in `marketplace.json` prüfen und analog bereinigen.

- [ ] **Step 6: MCP-Referenz aus `README.md` entfernen (H3)**

```bash
grep -n "MCP\|Model Context Protocol\|\.mcp\.json" README.md
```

Jede gefundene Zeile bewerten und streichen/umformulieren. Aktuelle README hat keinen aktiven MCP-Feature-Block, nur historische CHANGELOG-Einträge — die bleiben.

- [ ] **Step 7: Spec-Link in `README.md` ergänzen (L8)**

Am Ende von `README.md`, unter "## Links" (oder neu anlegen falls nicht vorhanden), den Link ergänzen:

```markdown
## Weiterführend

- [Anthropic Skill Spec](https://agentskills.io/specification) — offizielle Spec für Claude-Code-Skills
- [Plugin auf GitHub](https://github.com/jamski105/academic-research)
```

- [ ] **Step 8: Tests lokal laufen lassen**

```bash
cd ../academic-research-w1-a
python -m pytest tests/ -x -q
```

Expected: Alle bestehenden Tests grün.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4): Structural Hygiene (H2, H3, L1, L2, L8)

- settings.json (Root) gelöscht — wird von Claude Code nicht geladen
- .mcp.json (leer, Noise) gelöscht
- .pytest_cache aus Tracking + .gitignore-Eintrag
- owner.email/url in plugin.json + marketplace.json
- agentskills.io Spec-Link in README

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w1-a
```

---

### Task W1.B: Frontmatter Alignment

**Worktree:** `../academic-research-w1-b/` (Branch `v5.4-w1-b`)

**Files:**
- Modify: 13× `skills/*/SKILL.md` (Frontmatter Zeilen 1-5)
- Modify: `agents/query-generator.md`, `agents/relevance-scorer.md`
- Modify: `commands/history.md`, `commands/excel.md`

**Kritisch:** Nur `name:`, `description:`, `tools:`, `compatibility:`, `disable-model-invocation:`, `license:` Felder ändern. Body unverändert lassen (der gehört zu Wave 2).

- [ ] **Step 1: `name:` in allen 13 Skills auf kebab-case (H1)**

Für jeden Skill-Pfad unten: `name:`-Wert im YAML-Frontmatter ändern. Ordnername = Ziel-Name.

| Datei | Alt | Neu |
|---|---|---|
| `skills/abstract-generator/SKILL.md` | `Abstract Generator` | `abstract-generator` |
| `skills/academic-context/SKILL.md` | `Academic Context` | `academic-context` |
| `skills/advisor/SKILL.md` | `Advisor` | `advisor` |
| `skills/chapter-writer/SKILL.md` | `Chapter Writer` | `chapter-writer` |
| `skills/citation-extraction/SKILL.md` | `Citation Extraction` | `citation-extraction` |
| `skills/literature-gap-analysis/SKILL.md` | `Literature Gap Analysis` | `literature-gap-analysis` |
| `skills/methodology-advisor/SKILL.md` | `Methodology Advisor` | `methodology-advisor` |
| `skills/plagiarism-check/SKILL.md` | `Plagiarism Check` | `plagiarism-check` |
| `skills/research-question-refiner/SKILL.md` | `Research Question Refiner` | `research-question-refiner` |
| `skills/source-quality-audit/SKILL.md` | `Source Quality Audit` | `source-quality-audit` |
| `skills/style-evaluator/SKILL.md` | `Style Evaluator` | `style-evaluator` |
| `skills/submission-checker/SKILL.md` | `Submission Checker` | `submission-checker` |
| `skills/title-generator/SKILL.md` | `Title Generator` | `title-generator` |

- [ ] **Step 2: `tools: []` bei 2 Agents ergänzen (H6)**

In `agents/query-generator.md` und `agents/relevance-scorer.md` im Frontmatter ergänzen (nach `description:`, vor `---`):

```yaml
tools: []
```

Begründung: Beide Agents sind reine Textgenerierung ohne Tool-Bedarf.

- [ ] **Step 3: `compatibility:` bei 2 API-abhängigen Skills (M3)**

In `skills/chapter-writer/SKILL.md` und `skills/citation-extraction/SKILL.md` im Frontmatter ergänzen:

```yaml
compatibility: Claude API mit documents[] und citations.enabled
```

- [ ] **Step 4: `disable-model-invocation: true` in 2 Commands (M7)**

In `commands/history.md` und `commands/excel.md` im Frontmatter ergänzen:

```yaml
disable-model-invocation: true
```

Prüfen, ob die Commands schon ein Frontmatter-YAML haben; falls nicht, zwischen `---` eröffnen/schließen.

- [ ] **Step 5: `license:` Feld in allen 13 Skills (L9)**

In jedem der 13 `skills/*/SKILL.md` im Frontmatter ergänzen:

```yaml
license: MIT
```

- [ ] **Step 6: Test — Manifest-Tests laufen**

```bash
cd ../academic-research-w1-b
python -m pytest tests/test_manifest.py -v
```

Expected: Alle Tests grün. Falls ein Test `name` in Title-Case erwartet — dann Test anpassen im nächsten Step.

- [ ] **Step 7: Test — neuer Regression-Guard `test_skill_naming.py`**

Create `tests/test_skill_naming.py`:

```python
"""Regression guard: Skill-Namen sind kebab-case und stimmen mit Ordnernamen überein."""
import re
from pathlib import Path

import yaml

REPO_ROOT = Path(__file__).parent.parent
SKILLS_DIR = REPO_ROOT / "skills"
KEBAB_CASE = re.compile(r"^[a-z][a-z0-9]*(-[a-z0-9]+)*$")


def _load_frontmatter(skill_md: Path) -> dict:
    text = skill_md.read_text(encoding="utf-8")
    assert text.startswith("---\n"), f"Missing frontmatter in {skill_md}"
    end = text.index("\n---\n", 4)
    return yaml.safe_load(text[4:end])


def test_all_skill_names_are_kebab_case():
    for skill_dir in sorted(SKILLS_DIR.iterdir()):
        if not skill_dir.is_dir():
            continue
        skill_md = skill_dir / "SKILL.md"
        assert skill_md.exists(), f"Missing SKILL.md in {skill_dir}"
        fm = _load_frontmatter(skill_md)
        name = fm.get("name")
        assert name is not None, f"Missing name in {skill_md}"
        assert KEBAB_CASE.match(name), f"{skill_md}: name='{name}' is not kebab-case"


def test_skill_name_matches_directory():
    for skill_dir in sorted(SKILLS_DIR.iterdir()):
        if not skill_dir.is_dir():
            continue
        skill_md = skill_dir / "SKILL.md"
        fm = _load_frontmatter(skill_md)
        assert fm["name"] == skill_dir.name, (
            f"{skill_md}: name='{fm['name']}' != dir='{skill_dir.name}'"
        )
```

- [ ] **Step 8: Neuen Test laufen**

```bash
cd ../academic-research-w1-b
python -m pytest tests/test_skill_naming.py -v
```

Expected: 2 PASSED.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4): Frontmatter Alignment (H1, H6, M3, M7, L9)

- 13 Skill-Namen auf kebab-case (matches Ordnername)
- query-generator, relevance-scorer mit tools: []
- chapter-writer, citation-extraction mit compatibility:-Feld
- history, excel Commands mit disable-model-invocation: true
- license: MIT in allen 13 Skills
- Neuer Regression-Guard: tests/test_skill_naming.py

Cross-Referenzen (Title-Case in Prosa) werden in Wave 2 migriert.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w1-b
```

---

### Task W1.F: Agent Polish

**Worktree:** `../academic-research-w1-f/` (Branch `v5.4-w1-f`)

**Files:**
- Modify: `agents/quality-reviewer.md`

- [ ] **Step 1: ASCII-Umlaute zu echten Umlauten konvertieren (L4)**

`agents/quality-reviewer.md` durchgehen und alle ASCII-Ersätze fixen:

| ASCII | UTF-8 |
|---|---|
| `fuer` | `für` |
| `ueber` | `über` |
| `Begruendung` | `Begründung` |
| `Pruefen` / `prüfen` | `Prüfen` / `prüfen` |
| `Qualitaet` | `Qualität` |
| `Aktualitaet` | `Aktualität` |
| `zusaetzlich` | `zusätzlich` |
| `ausreichende` | `ausreichende` (schon korrekt) |
| `Laenge` | `Länge` |
| `Erklaerung` | `Erklärung` |
| `Pruefung` | `Prüfung` |

Befehl zum Finden aller ASCII-Umlaut-Kandidaten:
```bash
grep -nE "(fuer|ueber|Begruendung|Pruefen|Pruefung|Qualitaet|Aktualitaet|zusaetzlich|Laenge|Erklaerung|Kriterien)" agents/quality-reviewer.md
```

Jede Zeile durchgehen und manuell korrigieren. Aufpassen: `Kriterien` ist bereits korrekt, `Laenge` → `Länge`, aber `langen` unverändert.

- [ ] **Step 2: Regex-FP bei `\bwerden?\b` fixen (L5)**

In `agents/quality-reviewer.md` auf Zeile 99 (oder Position der Passiv-Detection-Regex):

Alt:
```
\bwerden?\b.*?(ge\w+|\w+iert)\b
```

Neu:
```
\bwerd(en|est|et)\b.*?(ge\w+|\w+iert)\b
```

Dies schließt "werde" (Ich-Form) aus, da Ich-Form meist Aktiv ist.

- [ ] **Step 3: Syntax-Check**

```bash
cd ../academic-research-w1-f
python -c "from pathlib import Path; t = Path('agents/quality-reviewer.md').read_text(encoding='utf-8'); print(f'ASCII residue: {sum(1 for w in [chr(252), chr(228), chr(246), chr(223)] if w in t)}/{len(t)} chars, heuristic check passed' if 'für' in t and 'Prüfen' in t else 'FAIL')"
```

Expected: "heuristic check passed".

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4): Agent Polish — quality-reviewer (L4, L5)

- ASCII-Umlaute (fuer, Begruendung, etc.) auf UTF-8-Umlaute konvertiert
- Passiv-Regex enger gefasst: \bwerden?\b → \bwerd(en|est|et)\b
  (schließt Ich-Form 'werde' als Aktiv-Kandidat aus)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w1-f
```

---

### Task W1.G: Directory Cleanup + Dead Code

**Worktree:** `../academic-research-w1-g/` (Branch `v5.4-w1-g`)

**Files:**
- Rename: `scripts/templates/` → `scripts/bootstrap/`
- Delete: `templates/literature_review.md`, `templates/research_report.md`, `templates/` (directory)
- Delete: `config/scoring.yaml`
- Modify: `scripts/project_bootstrap.py`, `commands/setup.md`, `README.md`

- [ ] **Step 1: `scripts/templates/` → `scripts/bootstrap/` umbenennen (L3)**

```bash
cd ../academic-research-w1-g
git mv scripts/templates scripts/bootstrap
```

- [ ] **Step 2: `scripts/project_bootstrap.py` Pfad-Konstante aktualisieren**

In `scripts/project_bootstrap.py`, die Zeile:
```python
TEMPLATES_DIR = Path(__file__).parent / "templates"
```
ändern zu:
```python
BOOTSTRAP_DIR = Path(__file__).parent / "bootstrap"
```

Und alle Referenzen auf `TEMPLATES_DIR` im File (inkl. Test-Monkeypatches) auf `BOOTSTRAP_DIR` umstellen.

- [ ] **Step 3: `tests/test_project_bootstrap.py` anpassen**

In `tests/test_project_bootstrap.py` alle `TEMPLATES_DIR`-Referenzen durch `BOOTSTRAP_DIR` ersetzen. Insbesondere die `monkeypatch.setattr(pb, "TEMPLATES_DIR", TEMPLATES)` → `monkeypatch.setattr(pb, "BOOTSTRAP_DIR", BOOTSTRAP)`.

- [ ] **Step 4: `templates/` (Root) löschen — Dead Code**

```bash
git rm -r templates/
```

Begründung: 0 Referenzen im aktiven Code (nur in `docs/superpowers/plans/` aus alten E-Tickets).

- [ ] **Step 5: `config/scoring.yaml` löschen — obsolet seit E2**

```bash
git rm config/scoring.yaml
```

Begründung: 5D-Scoring läuft seit E2 inline im `relevance-scorer`-Agent, `scoring.yaml` wird von keinem Skript/Skill mehr geladen.

- [ ] **Step 6: `README.md` aktualisieren**

Zeilen in `README.md` zu entfernen oder umformulieren:
- Zeile mit `config/scoring.yaml` als externe Konfiguration (Zeile ~376, ~426): streichen oder umformulieren auf "5D-Gewichtungen sind inline im relevance-scorer-Agent dokumentiert"
- Falls `templates/literature_review.md` oder `templates/research_report.md` im README auftauchen (über `grep`): ebenfalls streichen

```bash
grep -n "scoring.yaml\|templates/literature_review\|templates/research_report" README.md
```

Jede gefundene Zeile bewerten.

- [ ] **Step 7: `commands/setup.md` Scripts-Count aktualisieren**

`commands/setup.md` durchsuchen nach "4 Schritten" / "5 Schritten" / "6 Schritten" / Scripts-Zählungen und falls numerische Angaben auf gelöschte Files verweisen (z. B. "5 Scripts"), auf korrekte Zahl aktualisieren.

- [ ] **Step 8: venv-Pfad-Contract dokumentieren (L10)**

Im `README.md` unter "## Troubleshooting" oder "## Installation" den Abschnitt ergänzen:

```markdown
### Venv-Pfad-Contract

Die Commands `/search` und `/history` erwarten die Python-Venv unter
`~/.academic-research/venv/bin/python`. Dieser Pfad ist vom `setup.sh` fix
vorgegeben. Wer die Venv woanders anlegen möchte, kann `commands/search.md`
und `commands/history.md` entsprechend patchen.
```

- [ ] **Step 9: Tests laufen**

```bash
cd ../academic-research-w1-g
python -m pytest tests/ -x -q
```

Expected: Alle Tests grün, insbesondere `tests/test_project_bootstrap.py`.

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(v5.4): Directory Cleanup + Dead Code (L3, L10, C3-Teil)

- scripts/templates/ → scripts/bootstrap/ (Klarheit gegen top-level templates/)
- templates/ (Root) gelöscht — 0 Referenzen, toter Code
- config/scoring.yaml gelöscht — obsolet seit E2 (Inline-Scoring im Agent)
- scripts/project_bootstrap.py: TEMPLATES_DIR → BOOTSTRAP_DIR
- tests/test_project_bootstrap.py: Monkeypatch-Targets angepasst
- README.md: scoring.yaml-Referenzen entfernt, venv-Pfad-Contract dokumentiert

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w1-g
```

---

## Merge-Point 1

### Task MP1: Wave-1-Trees mergen

**Files:**
- Modify: Feature-Branch `v5.4-final-review` via Merge von 4 Branches

- [ ] **Step 1: Alle 4 Wave-1-Branches in Feature-Branch mergen**

```bash
cd /Users/j65674/Repos/academic-research
git checkout v5.4-final-review

for branch in v5.4-w1-a v5.4-w1-b v5.4-w1-f v5.4-w1-g; do
  echo "=== Merging $branch ==="
  git merge --no-ff "$branch" -m "Merge $branch into v5.4-final-review"
done
```

Expected: Keine Merge-Konflikte (Datei-Domänen sind disjunkt).

- [ ] **Step 2: Gesamtes Test-Suite**

```bash
python -m pytest tests/ -v
```

Expected: Alle Tests grün, inkl. `test_skill_naming.py`.

- [ ] **Step 3: Status-Check**

```bash
git log --oneline v5.3.0..HEAD
git status
```

Expected: 5 Commits über v5.3.0 hinaus (Baseline + 4 Merge-Commits plus Commits selbst). Clean Tree.

- [ ] **Step 4: Push Feature-Branch**

```bash
git push origin v5.4-final-review
```

**USER-REVIEW-GATE:** Hier pausieren. Dem User den Merge-Status zeigen, Freigabe holen, bevor Wave 2 startet.

---

## Wave 2: 4 Trees parallel (Skill-Shards)

**Shared Pattern für W2.S1–S4:** Jeder Subagent arbeitet in seinem Worktree und macht für jeden Skill seines Shards **alle** C+D+E-Änderungen komplett. Pro Skill folgenden Ablauf:

1. **`## Übersicht`-Block als erste H2 nach Titel einziehen** (M2) — 1-3 Sätze, was der Skill macht.
2. **`./`-Prefix für Kontext-Dateien setzen** (H7) — alle 3 Datei-Namen global im File.
3. **Cross-Referenzen** (D) — Title-Case-Skill-Namen in Prosa auf `` `kebab-case` `` umstellen.
4. **Few-Shots** (M1) — falls Skill in der M1-Liste: 2 Schlecht/Gut-Paare ergänzen nach Muster unten.

Das Muster für **Few-Shots**:

```markdown
## Few-Shot-Beispiele

### Stil: [Aspekt]

**Schlecht** (Grund: [warum problematisch]):

> [konkretes schlechtes Beispiel, 1-3 Zeilen]

**Gut** (Grund: [warum besser]):

> [konkretes gutes Beispiel, 1-3 Zeilen]
```

Der Subagent liest den konkreten Skill-Workflow und schreibt Schlecht/Gut-Paare, die typische Fehler beim Output dieses Skills zeigen (zu allgemein / zu spezifisch / Halluzination / fehlender Kontext).

---

### Task W2.S1: Skill-Shard 1 (3 Skills)

**Worktree:** `../academic-research-w2-s1/` (Branch `v5.4-w2-s1`)

**Skills:** `abstract-generator`, `academic-context`, `advisor`

- [ ] **Step 1: `abstract-generator/SKILL.md` bearbeiten**

  a. `## Übersicht` als erste H2 (M2) nach Titel einfügen:
  ```markdown
  ## Übersicht

  Erzeugt Abstract, Keywords und Management-Summary aus einer abgeschlossenen
  akademischen Arbeit. Liefert konkrete Formulierungen, keine Platzhalter,
  und respektiert Hochschul-Längenvorgaben.
  ```

  b. `./`-Prefix für alle 3 Kontext-Datei-Referenzen (H7): Alle Vorkommen von `` `academic_context.md` `` → `` `./academic_context.md` ``, analog für `literature_state.md` und `writing_state.md`.

  c. Cross-Refs (D): Grep auf Title-Case-Skill-Namen, alle auf `` `kebab-case` `` umstellen.

  d. Few-Shots (M1) ergänzen vor `## Wichtige Regeln`:
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Abstract (deskriptiv)

  **Schlecht** (Grund: Buzzwords ohne Inhalt, keine Methodik sichtbar):

  > "Diese Arbeit untersucht digitale Transformation in modernen Unternehmen
  > und liefert wichtige Erkenntnisse für die Zukunft."

  **Gut** (Grund: Forschungsfrage + Methodik + Ergebnis + Scope):

  > "Diese Arbeit untersucht, wie Cloud-Migration das IT-Service-Management
  > in deutschen KMU verändert. Mittels zehn semi-strukturierter Interviews
  > 2024 zeigt die Analyse drei zentrale Erfolgsfaktoren: iterative
  > Einführung, Change-Kommunikation und Lieferantendiversität."

  ### Stil: Keywords

  **Schlecht** (Grund: zu breit, nicht auffindbarkeitstauglich):

  > Digitalisierung, Unternehmen, Zukunft, KI

  **Gut** (Grund: spezifische Fachbegriffe, deutsche + englische Varianten):

  > Cloud-Migration, IT-Service-Management, KMU Deutschland,
  > Digitale Transformation, qualitative Interviews
  ```

- [ ] **Step 2: `academic-context/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Bootet den akademischen Kontext einer Arbeit: Thema, Forschungsfrage,
  Arbeitstyp, Methodik, Disziplin, Hochschule. Schreibt das Ergebnis in
  `./academic_context.md` als Single Source of Truth für alle anderen Skills.
  ```

  b. `## Vorbedingungen` (M5): Direkt nach `## Übersicht` einfügen:
  ```markdown
  ## Vorbedingungen

  Keine Vorbedingungen — dieser Skill bootet den Kontext. Alle anderen Skills
  setzen voraus, dass `./academic_context.md` existiert, und triggern diesen
  Skill bei Fehlen.
  ```

  c. Fenced-Block-Überschriften H2 → H3 (L7): Im Template-Markdown-Block (Zeilen ~66-88), wo `## Profil`, `## Arbeit`, `## Gliederung` stehen, auf `### Profil`, `### Arbeit`, `### Gliederung` ändern. Dies betrifft nur den Inhalt innerhalb des Fenced-Blocks.

  d. `./`-Prefix für alle Kontext-Datei-Referenzen (H7).

  e. Cross-Refs (D): Title-Case-Referenzen im Workflow auf kebab-case umstellen. Beispiel Zeile 104: "Wenn ein anderer Skill (Citation Extraction, Literature Gap Analysis, Advisor etc.)" → "Wenn ein anderer Skill (`citation-extraction`, `literature-gap-analysis`, `advisor` etc.)".

  f. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Forschungsfrage-Fixierung

  **Schlecht** (Grund: zu weit, nicht in Bachelor-Zeitrahmen beantwortbar):

  > "Wie wirkt sich Künstliche Intelligenz auf die Gesellschaft aus?"

  **Gut** (Grund: Scope + Kontext + messbare Dimensionen):

  > "Wie verändert der Einsatz generativer KI die Bewertungskriterien für
  > textbasierte Prüfungen an der FH Leibniz im Studienjahr 2024/25?"

  ### Stil: Methodik-Notation

  **Schlecht** (Grund: Oberbegriff ohne Operationalisierung):

  > Methodik: Qualitative Forschung

  **Gut** (Grund: Konkret + zählbar + reproduzierbar):

  > Methodik: 12 semi-strukturierte Interviews (je 45 min) mit
  > Lehrenden an drei FH-Standorten, Auswertung per Inhaltsanalyse
  > nach Mayring.
  ```

- [ ] **Step 3: `advisor/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Supervisor-Agent für die Gesamt-Gliederung einer Arbeit. Bewertet Struktur,
  schlägt Kapitel-Reorganisation vor, liefert Expose-Templates. Orchestriert
  andere Skills, wenn spezifische Themenbereiche vertieft werden müssen.
  ```

  b. `## Output-Format` als eigene H2 extrahieren (M4): Den Block, der aktuell inline im Quality-Review-Bereich (Zeilen ~150-167) steht, zu einer eigenen H2 `## Output-Format` vor `## Wichtige Regeln` verschieben.

  c. `./`-Prefix (H7).

  d. Cross-Refs (D).

  e. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Gliederungs-Bewertung

  **Schlecht** (Grund: generischer Kommentar ohne konkrete Aktion):

  > "Die Gliederung könnte strukturierter sein, arbeite nochmal daran."

  **Gut** (Grund: benennt konkretes Problem + gibt Fix):

  > "Kapitel 3.2 (Methodik) und Kapitel 4.1 (Datenerhebung) überlappen
  > inhaltlich. Ich schlage vor, 4.1 in 3.2 zu integrieren und Kapitel 4
  > auf die reine Ergebnispräsentation zu beschränken."
  ```

- [ ] **Step 4: Tests laufen**

```bash
cd ../academic-research-w2-s1
python -m pytest tests/ -x -q
```

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4-w2-s1): Skill-Shard 1 — abstract-generator, academic-context, advisor

- ## Übersicht-Section als erste H2 (M2)
- ./-Prefix für Kontext-Dateien (H7)
- Cross-Refs Title-Case → kebab-case (H1 Follow-up)
- Few-Shots in allen 3 Skills (M1)
- academic-context: ## Vorbedingungen-Block (M5), Fenced-Block H2→H3 (L7)
- advisor: ## Output-Format als eigene Section (M4)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w2-s1
```

---

### Task W2.S2: Skill-Shard 2 (3 Skills)

**Worktree:** `../academic-research-w2-s2/` (Branch `v5.4-w2-s2`)

**Skills:** `chapter-writer`, `citation-extraction`, `literature-gap-analysis`

- [ ] **Step 1: `chapter-writer/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Schreibt konkrete Kapitel (Einleitung, Theorieteil, Methodik, Empirie,
  Diskussion, Fazit) für akademische Arbeiten. Zieht Zitate aus
  `./literature_state.md` und folgt dem Disziplin-Register.
  ```

  b. **Trigger-Erweiterung (M6):** Die Skill-Description (Frontmatter) erweitern — aktuell triggert sie nur auf "Einleitung schreiben". Ergänzen um:
  - "Fazit schreiben", "Schlussteil"
  - "Methodik-Kapitel", "Methodenteil"
  - "Theorieteil", "Theoretischer Rahmen"
  - "Diskussion schreiben"
  - "Empirie schreiben", "Ergebnisse darstellen"

  Konkrete neue description (an bestehenden Stil anlehnen, alle Triggers in eine Liste):
  ```yaml
  description: Use this skill when the user writes a chapter of an academic work. Triggers on "Einleitung schreiben", "Theorieteil / Theoretischer Rahmen", "Methodik-Kapitel / Methodenteil", "Empirie / Ergebnisse darstellen", "Diskussion schreiben", "Fazit / Schlussteil", or when a chapter body with proper citations is needed. Schreibt vollständige Kapitelentwürfe; Für Abstract/Keywords → `abstract-generator`; für Zitations-Formatierung → `citation-extraction`.
  ```

  c. `./`-Prefix (H7).

  d. Cross-Refs (D).

  e. Few-Shots bereits vorhanden — prüfen und ggf. ergänzen, falls < 3 Beispiele. Aktueller Stand hat 3 Kapiteltypen mit je Schlecht/Gut; lassen wie ist.

- [ ] **Step 2: `citation-extraction/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Extrahiert und formatiert Zitate aus PDFs und Volltexten. Liefert
  Literaturverzeichnisse im Zitationsstil aus `./academic_context.md`
  (APA7, IEEE, Harvard etc.). Arbeitet eng mit der Claude-API
  `documents[] + citations.enabled`.
  ```

  b. **Trigger-Erweiterung (M6):** Description erweitern um "Literaturverzeichnis erstellen", "Literaturverzeichnis generieren". Kollision "chapter draft needs citation rendering" entzerren: Die Phrase auf "for pure citation rendering of extracted quotes" umformulieren, damit sie nicht mehr mit chapter-writer überlappt.

  Konkrete neue description:
  ```yaml
  description: Use this skill when the user needs to extract or format citations. Triggers on "Literaturverzeichnis prüfen / erstellen / generieren", "Bibliographie formatieren", "Zitation prüfen", "citation extraction", "bibliography generation", or when raw PDFs need citation rendering (not chapter body writing — for that → `chapter-writer`). Extrahiert Zitate aus PDFs und liefert formatierte Bibliographien im Zitationsstil aus `./academic_context.md`.
  ```

  c. `./`-Prefix (H7).

  d. Cross-Refs (D): insbesondere Zeile 186 (`"Literature Gap Analysis"` → `` `literature-gap-analysis` ``).

  e. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: APA7-Zitation

  **Schlecht** (Grund: fehlende Autor-Initialen, keine DOI, unvollständig):

  > Müller, T. (2023). Cloud-Migration in KMU.

  **Gut** (Grund: vollständige APA7-Notation mit DOI):

  > Müller, T., & Schmidt, A. (2023). Cloud-Migration in deutschen
  > KMU: Eine empirische Studie. *Zeitschrift für Wirtschaftsinformatik*,
  > 65(3), 215–234. https://doi.org/10.1007/s11576-023-00012-x

  ### Stil: Bibliography-Vollständigkeit

  **Schlecht** (Grund: Eintrag im Text ohne Literaturverzeichnis-Eintrag):

  > Text: "Müller (2023) argumentiert…"
  > Bibliography: (kein Müller-Eintrag)

  **Gut** (Grund: jeder In-Text-Zitat hat Bibliographie-Pendant):

  > Text: "Müller (2023) argumentiert…"
  > Bibliography: Müller, T. (2023). *Titel*. Verlag. DOI.
  ```

- [ ] **Step 3: `literature-gap-analysis/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Analysiert die Literaturbasis auf Abdeckungslücken (fehlende Themen,
  Autor*innen-Gruppen, Methoden, Zeiträume). Identifiziert disziplinäre
  Blindstellen und liefert konkrete Such-Queries für `/search`. Ergänzt
  den `source-quality-audit` (der bewertet Einzelquellen, nicht Korpus).
  ```

  b. `## Output-Format` als eigene H2 extrahieren (M4): Den Block Zeilen ~118-138 zu einer eigenen H2 direkt vor `## Wichtige Regeln` verschieben.

  c. `./`-Prefix (H7).

  d. Cross-Refs (D): Zeile 67 (`"Advisor, Chapter Writer, Citation Extraction"` → `` `advisor`, `chapter-writer`, `citation-extraction` ``).

  e. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Gap-Identifikation

  **Schlecht** (Grund: vage ohne konkrete Lücke):

  > "Es fehlen noch einige Quellen zu KI und Lehre."

  **Gut** (Grund: konkretes Konzept + Zahl + Such-Empfehlung):

  > "Unterversorgt: 'formative Bewertung mit KI' — nur 1 Quelle im Korpus.
  > Empfehlung: /search 'formative assessment AI higher education 2023-2024'
  > mit Modulen semanticscholar + openalex."

  ### Stil: Corpus-Coverage

  **Schlecht** (Grund: unklar was abgedeckt ist):

  > "Literaturbasis ist okay."

  **Gut** (Grund: Coverage pro Forschungsfrage-Dimension):

  > "Abdeckung Forschungsfrage 1 (KI-Adoption): 12 Quellen OK.
  > Abdeckung Nebenfrage 2 (ethische Implikationen): 2 Quellen UNTER-SERVIERT."
  ```

- [ ] **Step 4: Tests laufen**

```bash
cd ../academic-research-w2-s2
python -m pytest tests/ -x -q
```

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4-w2-s2): Skill-Shard 2 — chapter-writer, citation-extraction, literature-gap-analysis

- ## Übersicht-Section als erste H2 (M2)
- ./-Prefix für Kontext-Dateien (H7)
- Cross-Refs Title-Case → kebab-case (H1 Follow-up)
- chapter-writer: Trigger-Erweiterung (Fazit/Methodik/Theorie/Diskussion) (M6)
- citation-extraction: Trigger-Erweiterung (Literaturverzeichnis erstellen/generieren) + Kollision mit chapter-writer entzerrt (M6)
- citation-extraction: Few-Shots ergänzt (M1)
- literature-gap-analysis: ## Output-Format eigene Section (M4), Few-Shots (M1)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w2-s2
```

---

### Task W2.S3: Skill-Shard 3 (4 Skills)

**Worktree:** `../academic-research-w2-s3/` (Branch `v5.4-w2-s3`)

**Skills:** `methodology-advisor`, `plagiarism-check`, `research-question-refiner`, `source-quality-audit`

- [ ] **Step 1: `methodology-advisor/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Berät bei der Methodenwahl: qualitativ, quantitativ, Mixed-Methods, oder
  spezielle Verfahren (Delphi, SLR, Grounded Theory). Mappt Forschungsfrage
  auf geeignete Methoden und skizziert Operationalisierung. Nicht zuständig
  für Schärfung der Forschungsfrage (→ `research-question-refiner`).
  ```

  b. **`## Abgrenzung`-Block einziehen (H5)** zwischen `## Keine Fabrikation` und `## Methoden-Scoring-Matrix`:
  ```markdown
  ## Abgrenzung

  Wählt und begründet Methoden passend zur Forschungsfrage.
  Für Schärfung der Forschungsfrage selbst → `research-question-refiner`.
  Für Einbettung in die Gliederung → `advisor`.
  ```

  c. `./`-Prefix (H7).

  d. Cross-Refs (D).

  e. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Fragentyp → Methode

  **Schlecht** (Grund: Methode ohne Passung zum Fragentyp):

  > Forschungsfrage: "Welche Faktoren erklären KI-Adoption in KMU?"
  > Methode: Experimentelles Design mit Kontrollgruppe.

  **Gut** (Grund: Methode folgt Fragentyp und Datenlage):

  > Forschungsfrage: "Welche Faktoren erklären KI-Adoption in KMU?"
  > Methode: Qualitative Interviews (8–12) + qualitative Inhaltsanalyse,
  > weil explorativer Fragentyp und geringe Vorstrukturierung bestehender
  > Theorie.
  ```

- [ ] **Step 2: `plagiarism-check/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Prüft Textähnlichkeit gegen die Quellen in `./literature_state.md`.
  Markiert wörtliche Übernahmen und paraphrasierte Passagen, die nicht
  ausreichend zitiert sind. Nicht zuständig für Stil-Bewertung allgemein
  (→ `style-evaluator`).
  ```

  b. `./`-Prefix (H7).

  c. Cross-Refs (D): Zeile 39 (`"Chapter Writer"` → `` `chapter-writer` ``).

  d. **`text_utils.py`-Referenz rechtfertigen (L6):** Den Abschnitt Zeile ~49, der `text_utils.py` referenziert, prüfen. Wenn die Funktionen dort deterministische Tokenisierung sind, die Claude nicht zuverlässig inline machen kann (z. B. n-gram-Matching), dann **Rechtfertigungs-Satz ergänzen**:
  ```markdown
  Hinweis: `text_utils.py` liefert deterministische Tokenisierung und
  n-gram-Matching. Inline-Berechnung wäre unzuverlässig (Reproducibility).
  ```

  Andernfalls (wenn inline machbar): Referenz entfernen und Logik inline beschreiben. Entscheidung durch den Subagent basierend auf der tatsächlichen Nutzung.

  e. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Paraphrase-Erkennung

  **Schlecht** (Grund: "Ähnlichkeit" ohne Threshold/Evidenz):

  > "Dieser Abschnitt wirkt etwas ähnlich zu Müller 2023."

  **Gut** (Grund: Satz-genauer Match mit Quelle):

  > "Zeile 42-44 zeigt 7-Wort-Match zu Müller (2023, S. 214):
  > 'Cloud-Migration verlangt iterative Einführung' → unzitiert.
  > Empfehlung: Direktzitat mit Seitenangabe oder Paraphrase."
  ```

- [ ] **Step 3: `research-question-refiner/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Verfeinert bestehende Forschungsfragen auf Spezifität, Beantwortbarkeit,
  Falsifizierbarkeit und Einzigartigkeit. Liefert 2-3 Alternativen je
  Problem-Typ (zu weit / zu eng / nicht falsifizierbar / mehrdimensional).
  ```

  b. `./`-Prefix (H7). **Die description nutzt bereits kebab-case** (`academic-context`) — vorhandene Prosa prüfen, was mehr ist.

  c. Cross-Refs (D).

  d. Few-Shots bereits vorhanden (Zu eng / Zu weit / Nicht-falsifizierbar / Mehrdimensional) — prüfen und ergänzen falls < 4 Beispiele. Aktuell 4 Templates — ausreichend.

- [ ] **Step 4: `source-quality-audit/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Bewertet Qualität, Balance, Aktualität und Diversität der Literaturbasis
  einer Arbeit. Scort 5 Dimensionen (0-100), identifiziert Schwächen,
  liefert priorisierte Verbesserungsempfehlungen. Fokus auf Einzelquellen
  — Korpus-Coverage macht `literature-gap-analysis`.
  ```

  b. `./`-Prefix (H7).

  c. Cross-Refs (D).

  d. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Peer-Review-Bewertung

  **Schlecht** (Grund: pauschale Aussage ohne Klassifikation):

  > "Quellen sehen ganz OK aus."

  **Gut** (Grund: Dimensionaler Score mit konkretem Problem):

  > "Peer-Review-Anteil: 62 % (Score 78). WARN: 3 nicht-akademische
  > Web-Quellen (Blog-Post, Unternehmens-Whitepaper) ohne
  > institutionelle Autorität — durch peer-reviewte Alternativen
  > ersetzen."

  ### Stil: Aktualitäts-Bewertung

  **Schlecht** (Grund: ignoriert Grundlagenwerke):

  > "Porter 1985 ist alt, bitte aktuellere Quelle nutzen."

  **Gut** (Grund: erkennt Standardwerk-Status):

  > "Porter (1985) ist als Grundlagenwerk markiert — von Aktualitäts-
  > Abzügen ausgenommen. Kernquellen nach 2020 liegen bei 55 % → Score 82."
  ```

- [ ] **Step 5: Tests laufen**

```bash
cd ../academic-research-w2-s3
python -m pytest tests/ -x -q
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4-w2-s3): Skill-Shard 3 — methodology-advisor, plagiarism-check, research-question-refiner, source-quality-audit

- ## Übersicht-Section als erste H2 (M2)
- ./-Prefix für Kontext-Dateien (H7)
- Cross-Refs Title-Case → kebab-case (H1 Follow-up)
- methodology-advisor: ## Abgrenzung-Block (H5), Few-Shots (M1)
- plagiarism-check: text_utils.py-Referenz gerechtfertigt (L6), Few-Shots (M1)
- source-quality-audit: Few-Shots (M1)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w2-s3
```

---

### Task W2.S4: Skill-Shard 4 + Docs (3 Skills + Agents + README)

**Worktree:** `../academic-research-w2-s4/` (Branch `v5.4-w2-s4`)

**Skills:** `style-evaluator`, `submission-checker`, `title-generator`
**Plus:** Agents Cross-Refs, README Cross-Refs

- [ ] **Step 1: `style-evaluator/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Bewertet Menschlichkeit, KI-Detektions-Anfälligkeit, Kohärenz, Duplikation
  und akademische Qualität von Text. Gewichteter 0-100-Score über 5 Dimensionen.
  Bietet Rewrite-Vorschläge für geflaggte Stellen ohne Bedeutung zu verlieren.
  ```

  b. `./`-Prefix (H7).

  c. Cross-Refs (D): Zeile 53 (`"Chapter Writer"` → `` `chapter-writer` ``).

  d. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Anti-KI-Detektion

  **Schlecht** (Grund: uniforme Satzlänge, erwartbare Transitionen):

  > "Darüber hinaus ist Digitalisierung wichtig. Furthermore können
  > Unternehmen profitieren. Moreover ist Geschwindigkeit ein Faktor."

  **Gut** (Grund: variierte Syntax, natürliche Hedges):

  > "Digitalisierung bleibt zentral. Unternehmen profitieren — allerdings
  > nur, wenn Geschwindigkeit nicht das einzige Kriterium ist."

  ### Stil: Rewrite mit Argument-Erhalt

  **Schlecht** (Grund: Rewrite verändert Argumentation):

  > Original: "Die Stichprobe ist klein (n=8), daher explorativ."
  > Rewrite: "Die Stichprobe ist klein, aber repräsentativ."

  **Gut** (Grund: Rewrite erhält Argumentation, variiert Struktur):

  > Original: "Die Stichprobe ist klein (n=8), daher explorativ."
  > Rewrite: "Mit n=8 bleibt die Studie explorativ — die Ergebnisse
  > skizzieren Hypothesen, sie prüfen sie nicht."
  ```

- [ ] **Step 2: `submission-checker/SKILL.md` bearbeiten (H4 komplett)**

  a. Legacy-Datei löschen:
  ```bash
  cd ../academic-research-w2-s4
  git rm skills/submission-checker/leibniz-fh-requirements.md
  ```

  b. Im `SKILL.md` alle Referenzen auf `leibniz-fh-requirements.md` (Zeilen 50, 79, 145, 152, 189) umstellen auf den Variant-Selector:
  - `${CLAUDE_PLUGIN_ROOT}/skills/submission-checker/leibniz-fh-requirements.md` → `${CLAUDE_PLUGIN_ROOT}/skills/submission-checker/references/fh-leibniz.md`
  - Analog bei Variant-abhängigen Referenzen: den Variant-Selector (Zeilen 34-45 im SKILL.md) durchgängig anwenden.

  c. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Prüft Formalia vor Abgabe: Pflichtabschnitte, Seitenumfang, Formatierung,
  Quellenzahl, Abbildungen/Tabellen, eidesstattliche Erklärung. Verwendet
  hochschulspezifische Regeln aus `references/<variant>.md` (Default:
  FH Leibniz).
  ```

  d. **`## Abgrenzung`-Block einziehen (H4):** Nach `## Keine Fabrikation`:
  ```markdown
  ## Abgrenzung

  Prüft Formalia der Enddatei gegen Hochschul-Regeln.
  Für Abstract, Keywords, Management-Summary → `abstract-generator`.
  Für den Titel selbst → `title-generator`.
  Für Kontextdaten (Arbeitstyp, Hochschule) → `academic-context`.
  ```

  e. `./`-Prefix (H7).

  f. Cross-Refs (D).

  g. Few-Shots (M1):
  ```markdown
  ## Few-Shot-Beispiele

  ### Stil: Formalia-Bewertung

  **Schlecht** (Grund: PASS ohne dokumentierte Prüfung):

  > "Formatierung ist OK."

  **Gut** (Grund: dimensionaler Score mit konkretem Prüfpunkt):

  > "Formatierung: PARTIAL. Zeilenabstand 1.0 statt geforderten 1.5
  > (Seiten 12-18). Fix: Absätze 12-18 markieren, Zeilenabstand in
  > Formatvorlage 'Standard' auf 1.5 setzen."
  ```

- [ ] **Step 3: `title-generator/SKILL.md` bearbeiten**

  a. `## Übersicht` (M2):
  ```markdown
  ## Übersicht

  Schlägt 5-7 Titelvarianten vor (klassisch-akademisch, fragenbasiert,
  kreativ, ergebnisorientiert) mit Rationale und Stärke/Einschränkung.
  Basiert auf `./writing_state.md` (fertiger Text) und `./academic_context.md`
  (Forschungsfrage). Keine leeren Titel-Hülsen.
  ```

  b. `./`-Prefix (H7).

  c. Cross-Refs (D).

  d. Few-Shots bereits vorhanden (Deskriptiv / These / Frage) — ausreichend.

- [ ] **Step 4: Agents Cross-Refs (D)**

  Alle `agents/*.md` durchsuchen auf Title-Case-Skill-Namen:

  ```bash
  cd ../academic-research-w2-s4
  grep -rn -E "(Abstract Generator|Academic Context|Chapter Writer|Citation Extraction|Literature Gap Analysis|Source Quality Audit|Research Question Refiner|Submission Checker|Style Evaluator|Title Generator|Methodology Advisor|Plagiarism Check)" agents/
  ```

  Jede gefundene Zeile auf `` `kebab-case` ``-Form anpassen.

- [ ] **Step 5: README Cross-Refs (D)**

  ```bash
  grep -n -E "(Abstract Generator|Academic Context|Chapter Writer|Citation Extraction|Literature Gap Analysis|Source Quality Audit|Research Question Refiner|Submission Checker|Style Evaluator|Title Generator|Methodology Advisor|Plagiarism Check)" README.md
  ```

  Aufpassen: In der Skills-Tabelle kann der Title Case als Anzeigename sinnvoll sein. **Regel:** In Fließtext und Skill-Referenzen → `` `kebab-case` ``; in der Skills-Tabelle als Anzeigename → Title Case behalten, aber ergänze eine Spalte mit dem kebab-case-Namen oder nutze nur kebab-case.

  Empfehlung: Alle Skills-Tabellen-Einträge auf kebab-case (Einheitlichkeit mit den tatsächlichen `name:`-Feldern).

- [ ] **Step 6: Neuer Regression-Guard `test_cross_references.py`**

Create `tests/test_cross_references.py`:

```python
"""Regression guard: Keine Title-Case-Skill-Namen in Prosa."""
import re
from pathlib import Path

REPO_ROOT = Path(__file__).parent.parent
TITLE_CASE_NAMES = [
    "Abstract Generator",
    "Academic Context",
    "Chapter Writer",
    "Citation Extraction",
    "Literature Gap Analysis",
    "Source Quality Audit",
    "Research Question Refiner",
    "Submission Checker",
    "Style Evaluator",
    "Title Generator",
    "Methodology Advisor",
    "Plagiarism Check",
]
SCAN_DIRS = ["skills", "agents", "commands"]
SCAN_FILES = ["README.md"]


def _iter_files():
    for d in SCAN_DIRS:
        yield from (REPO_ROOT / d).rglob("*.md")
    for f in SCAN_FILES:
        yield REPO_ROOT / f


def test_no_title_case_skill_names_in_prose():
    patterns = [re.compile(rf"\b{re.escape(name)}\b") for name in TITLE_CASE_NAMES]
    violations = []
    for path in _iter_files():
        if not path.exists():
            continue
        text = path.read_text(encoding="utf-8")
        for name, pattern in zip(TITLE_CASE_NAMES, patterns):
            for match in pattern.finditer(text):
                line_num = text[: match.start()].count("\n") + 1
                violations.append(f"{path.relative_to(REPO_ROOT)}:{line_num}: '{name}'")
    assert not violations, (
        "Title-Case-Skill-Namen in Prosa gefunden:\n"
        + "\n".join(violations[:20])
        + (f"\n... und {len(violations) - 20} mehr" if len(violations) > 20 else "")
    )
```

- [ ] **Step 7: Test laufen**

```bash
cd ../academic-research-w2-s4
python -m pytest tests/test_cross_references.py -v
```

Expected: PASSED (keine Title-Case-Referenzen mehr).

- [ ] **Step 8: Alle Tests laufen**

```bash
python -m pytest tests/ -x -q
```

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
fix(v5.4-w2-s4): Skill-Shard 4 + Docs — style-evaluator, submission-checker, title-generator

- ## Übersicht-Section als erste H2 (M2)
- ./-Prefix für Kontext-Dateien (H7)
- Cross-Refs Title-Case → kebab-case in Skills, Agents, README (H1 Follow-up, D)
- style-evaluator: Few-Shots (M1)
- submission-checker: Legacy-Datei leibniz-fh-requirements.md gelöscht (H4),
  Variant-Selector aktiviert, ## Abgrenzung-Block (H4), Few-Shots (M1)
- title-generator: Few-Shots (ausreichend vorhanden)
- Neuer Regression-Guard: tests/test_cross_references.py

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin v5.4-w2-s4
```

---

## Merge-Point 2

### Task MP2: Wave-2-Trees mergen

**Files:**
- Modify: Feature-Branch via Merge

- [ ] **Step 1: Alle 4 Wave-2-Branches in Feature-Branch mergen**

```bash
cd /Users/j65674/Repos/academic-research
git checkout v5.4-final-review

for branch in v5.4-w2-s1 v5.4-w2-s2 v5.4-w2-s3 v5.4-w2-s4; do
  echo "=== Merging $branch ==="
  git merge --no-ff "$branch" -m "Merge $branch into v5.4-final-review"
done
```

Expected: Keine Konflikte (Skill-Shards disjunkt).

- [ ] **Step 2: Verification — Baseline-Grep sollte leer sein**

```bash
grep -rn -E "(Abstract Generator|Academic Context|Chapter Writer|Citation Extraction|Literature Gap Analysis|Source Quality Audit|Research Question Refiner|Submission Checker|Style Evaluator|Title Generator|Methodology Advisor|Plagiarism Check)" skills/ agents/ commands/ README.md 2>/dev/null | grep -v ".git" | grep -v "CHANGELOG"
```

Expected: 0 Zeilen Output (alle Title-Case-Skill-Namen migriert).

- [ ] **Step 3: Verification — ./-Prefix-Inkonsistenz weg**

```bash
grep -rn -E "\`(academic_context|literature_state|writing_state)\.md\`" skills/ 2>/dev/null | grep -v "\./"
```

Expected: 0 Zeilen Output.

- [ ] **Step 4: Gesamtes Test-Suite**

```bash
python -m pytest tests/ -v
```

Expected: Alle Tests grün, inkl. `test_skill_naming.py` und `test_cross_references.py`.

- [ ] **Step 5: Push**

```bash
git push origin v5.4-final-review
```

**USER-REVIEW-GATE:** Hier pausieren. Diff zum User zeigen, Freigabe holen, bevor Wave 3 startet.

---

## Wave 3: Sequenzielle Cleanup

### Task W3.H: Repo Hygiene

**Worktree:** `../academic-research-w3-h/` (Branch `v5.4-w3-h`)

**Worktree erst jetzt anlegen, da es von Wave-2-Merges abhängt:**

```bash
cd /Users/j65674/Repos/academic-research
git worktree add ../academic-research-w3-h -b v5.4-w3-h v5.4-final-review
```

**Files:**
- Modify: `.gitignore`
- Remove from tracking: `docs/superpowers/specs/` + `docs/superpowers/plans/`

- [ ] **Step 1: `.gitignore` erweitern (C2)**

In `.gitignore` am Ende einen Block ergänzen:

```
# Brainstorming-Artefakte (persönliche Arbeitsprodukte, nicht Release-Inhalt)
docs/superpowers/specs/
docs/superpowers/plans/
```

- [ ] **Step 2: `docs/superpowers/specs/` + `plans/` aus Tracking entfernen (C1)**

```bash
cd ../academic-research-w3-h
git rm --cached -r docs/superpowers/specs/
git rm --cached -r docs/superpowers/plans/
```

Expected: Dateien auf der Platte sollen **bleiben**, nur das Git-Tracking endet. Mit `ls docs/superpowers/specs/` prüfen.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
chore(v5.4): Repo Hygiene — docs/superpowers/ aus Tracking (C1, C2)

- .gitignore: docs/superpowers/specs/ + plans/ ergänzt
- git rm --cached -r für beide Verzeichnisse (lokal bleiben sie erhalten)

Brainstorming-Artefakte sind persönliche Arbeitsprodukte und gehören
nicht ins Plugin-Release. Analog zu .claude/ (bereits gitignored).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Smoke-Test in frischem tmp-Ordner (C4)**

```bash
TMPDIR=$(mktemp -d)
cd "$TMPDIR"
echo "Test-Ordner: $TMPDIR"

# /setup simulieren: venv + bootstrap
# (Annahme: setup.sh ist via Plugin-Root erreichbar)
bash ~/.claude/plugins/cache/claude-plugins-official/academic-research/*/scripts/setup.sh || echo "Setup-Script-Pfad prüfen"

# Prüfen, was angelegt wurde
ls -la
test -d kapitel && echo "OK: kapitel/"
test -d literatur && echo "OK: literatur/"
test -d pdfs && echo "OK: pdfs/"
test -f CLAUDE.md && echo "OK: CLAUDE.md"
test -f academic_context.md && echo "OK: academic_context.md"
```

Expected: Alle 5 OK-Meldungen.

**Hinweis:** Falls Setup nicht in einem Subshell läuft (z. B. Plugin-Root-Variable nicht gesetzt), manuell `scripts/project_bootstrap.py` ausführen:

```bash
python /Users/j65674/Repos/academic-research-w3-h/scripts/project_bootstrap.py
```

- [ ] **Step 5: Finaler `git status` (C5)**

```bash
cd ../academic-research-w3-h
git status
```

Expected: `working tree clean` oder nur untracked `docs/superpowers/specs/*.md` + `docs/superpowers/plans/*.md` (die lokal bleiben, aber ab jetzt ignoriert sind).

- [ ] **Step 6: Push**

```bash
git push -u origin v5.4-w3-h
```

- [ ] **Step 7: Merge in Feature-Branch**

```bash
cd /Users/j65674/Repos/academic-research
git checkout v5.4-final-review
git merge --no-ff v5.4-w3-h -m "Merge v5.4-w3-h (Repo Hygiene) into v5.4-final-review"
git push origin v5.4-final-review
```

---

## Release-Phase

### Task RELEASE: Version Bump + CHANGELOG + PR + Tag

**Files:**
- Modify: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CHANGELOG.md`, `README.md`

- [ ] **Step 1: Version-Bump in beiden Manifesten**

`.claude-plugin/plugin.json` und `.claude-plugin/marketplace.json`:
```json
"version": "5.4.0"
```

- [ ] **Step 2: CHANGELOG-Block ergänzen**

In `CHANGELOG.md` ganz oben nach `# Changelog` einen neuen Block:

```markdown
## [5.4.0] — 2026-04-24

Finale Review-Runde gegen anthropics/skills Cookbook (Commit 5128e186).
29 Findings aus Tri-Agent-Review in einem Release.

### Changed

- Skill-Namen auf kebab-case (alle 13 Skills): `Abstract Generator` → `abstract-generator` etc. Matches Ordnername (Anthropic-Konvention).
- `./`-Prefix für Kontext-Datei-Referenzen durchgängig (`./academic_context.md`, `./literature_state.md`, `./writing_state.md`).
- `## Übersicht` als erste H2 in allen 13 Skills (Cookbook-Pattern).
- Few-Shot-Beispiele (Schlecht/Gut) in 10 bisher few-shot-losen Skills.
- Skill-Cross-Referenzen in Prosa: Title Case → `` `kebab-case` ``.
- `chapter-writer`, `citation-extraction` Trigger erweitert (Fazit/Methodik/Theorie/Literaturverzeichnis-erstellen).

### Fixed

- `submission-checker`: Legacy-Datei `leibniz-fh-requirements.md` entfernt, Variant-Selector (`references/<variant>.md`) aktiviert, `## Abgrenzung`-Block ergänzt.
- `methodology-advisor`: Fehlender `## Abgrenzung`-Block ergänzt.
- `agents/query-generator.md`, `agents/relevance-scorer.md`: `tools: []` Allowlist ergänzt (vorher: erbten alle Tools).
- `commands/history.md`, `commands/excel.md`: `disable-model-invocation: true` ergänzt (vorher: auto-invocation möglich).
- `agents/quality-reviewer.md`: ASCII-Umlaute zu echten Umlauten, Passiv-Regex enger gefasst (`\bwerden?\b` → `\bwerd(en|est|et)\b`).

### Removed

- `settings.json` (Root): wurde von Claude Code nicht geladen.
- `.mcp.json` (Root): leer, Noise.
- `templates/` (Root): toter Code, 0 Referenzen.
- `config/scoring.yaml`: obsolet seit E2 (Scoring läuft inline im Agent).
- `skills/submission-checker/leibniz-fh-requirements.md`: Legacy, durch Variant-Selector ersetzt.

### Internal

- `scripts/templates/` → `scripts/bootstrap/` umbenannt (Klarheit).
- `compatibility:`-Frontmatter-Feld bei API-abhängigen Skills.
- `license: MIT` in allen 13 Skills.
- `owner.email` + `owner.url` in beiden Manifesten.
- `docs/superpowers/specs/` + `plans/` aus Git-Tracking entfernt (via `.gitignore`, lokal bleiben).
- `.pytest_cache/` aus Tracking + `.gitignore`.
- 2 neue Regression-Guards: `tests/test_skill_naming.py`, `tests/test_cross_references.py`.
- Spec-Link `https://agentskills.io/specification` im README.
- Venv-Pfad-Contract (`~/.academic-research/venv/`) im README dokumentiert.
```

- [ ] **Step 3: README-Update**

In `README.md`:
- "Was ist neu"-Tabelle: Neue Zeile v5.4.0 mit Zusammenfassung.
- Skills-Tabelle: Sicherstellen, dass alle 13 Skills mit kebab-case-Name + Anzeigename gelistet sind.
- Scripts-Liste: Falls dort "4 Scripts" oder "5 Scripts" steht — korrigieren auf Ist-Stand nach Löschungen/Rename.

- [ ] **Step 4: Commit Version-Bump**

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json CHANGELOG.md README.md
git commit -m "$(cat <<'EOF'
chore(v5.4): bump version 5.3.0 → 5.4.0 + CHANGELOG

29 Findings aus Tri-Agent-Review:
- 7 HIGH (Konvention, Struktur, Agent-Allowlist)
- 7 MEDIUM (Few-Shots, Übersicht, Trigger, Commands)
- 10 LOW (Cleanup, Polish)
- 5 Cleanup (Repo-Hygiene, Dead Code)

Kein Verhaltens-Change für Endnutzer. Minor-Bump wegen Konventions-Migration.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push origin v5.4-final-review
```

- [ ] **Step 5: PR erstellen**

```bash
gh pr create --base main --head v5.4-final-review \
  --title "refactor: v5.4 Final Review — Cookbook Alignment + Cleanup" \
  --body "$(cat <<'EOF'
## Summary

- 29 Findings aus Tri-Agent-Review (plugin-validator, Cross-Skill-Consistency, Cookbook-Gap-Analyse gegen `anthropics/skills @ 5128e186`) in einem Release
- Konventions-Alignment: Skill-Namen auf kebab-case, `./`-Prefix einheitlich, `## Übersicht`-Section in allen 13 Skills
- Cleanup: Dead Code (`templates/`, `config/scoring.yaml`, `settings.json`, `.mcp.json`) entfernt, `docs/superpowers/` aus Tracking

## Test plan

- [ ] `pytest tests/ -v` zeigt alle Tests grün (inkl. neuer Regression-Guards `test_skill_naming.py` + `test_cross_references.py`)
- [ ] Smoke-Test in frischem tmp-Ordner: `/setup` + `/search` erfolgreich
- [ ] `grep -rn "Title Case Skill Names"` in Skills/Agents/README liefert 0 Zeilen
- [ ] Version-Bump in `plugin.json` + `marketplace.json` auf `5.4.0`
- [ ] CHANGELOG-Block `[5.4.0] — 2026-04-24` mit 4 Abschnitten

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 6: PR mergen (squash) nach User-Review-Freigabe**

```bash
gh pr merge --squash --delete-branch
```

- [ ] **Step 7: Tag v5.4.0 setzen und pushen**

```bash
cd /Users/j65674/Repos/academic-research
git checkout main
git pull
MERGE_COMMIT=$(git log --format="%H" -1)
git tag -a v5.4.0 -m "Release v5.4.0 — Final Review: Cookbook Alignment + Cleanup" "$MERGE_COMMIT"
git push origin v5.4.0
```

- [ ] **Step 8: Worktrees aufräumen**

```bash
for tree in w1-a w1-b w1-f w1-g w2-s1 w2-s2 w2-s3 w2-s4 w3-h; do
  git worktree remove "../academic-research-${tree}" 2>/dev/null || true
done
git worktree prune
git worktree list
```

Expected: Nur noch main-Worktree.

- [ ] **Step 9: Local Branch-Cleanup**

```bash
for branch in v5.4-w1-a v5.4-w1-b v5.4-w1-f v5.4-w1-g v5.4-w2-s1 v5.4-w2-s2 v5.4-w2-s3 v5.4-w2-s4 v5.4-w3-h v5.4-final-review; do
  git branch -D "$branch" 2>/dev/null || true
done
git fetch --prune
```

---

## Zusammenfassung

**Tasks total:** 14 (0, W1.A, W1.B, W1.F, W1.G, MP1, W2.S1, W2.S2, W2.S3, W2.S4, MP2, W3.H, RELEASE)
**Git-Commits:** ~13 (Baseline + 8 Tree-Commits + 3 Merge-Commits + Version-Bump)
**Neue Tests:** 2 (`test_skill_naming.py`, `test_cross_references.py`)
**Gelöschte Dateien:** 5 (settings.json, .mcp.json, templates/literature_review.md, templates/research_report.md, config/scoring.yaml) + Legacy-Requirements

**Parallelisierungs-Gewinn:** Wave 1 4× parallel + Wave 2 4× parallel statt 8× sequenziell → geschätzt 60 % Zeitersparnis bei ausreichend vielen Subagent-Slots.
