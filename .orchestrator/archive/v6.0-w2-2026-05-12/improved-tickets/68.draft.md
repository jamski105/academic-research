## Context

`chapter-writer` produziert aktuell Kapitel-Drafts, die direkt an `quality-reviewer` übergeben werden, ohne einen Anti-KI-Audit-Pass zwischenzuschalten. Bei Hochschularbeiten (Bachelor, Master, Diplom, Dissertation) ist KI-Detektor-Auffälligkeit ein ernstes Risiko (Turnitin, GPTZero, OriginalityAI). Der bereits vendorierte `humanizer-de`-Skill (`~/.codex/skills/humanizer-de/`) soll daher als Pflicht-Zwischenschritt in die `chapter-writer`-Pipeline integriert werden — allerdings nur für Hochschulkontexte und opt-out-fähig.

Bezug: [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §7 (F4), v6.0 Sprint 1. Schließt (TEMP) #59 (Humanizer-Referenz: https://github.com/marmbiz/humanizer-de#).

---

## Acceptance Criteria

- [ ] `skills/chapter-writer/SKILL.md` ruft `humanizer-de` im Mode `audit` auf, **bevor** `quality-reviewer` aufgerufen wird (`draft → humanizer-de(audit) → quality-reviewer → final`)
- [ ] Der Humanizer-Schritt wird nur ausgelöst, wenn `output_target` in `./academic_context.md` einen der Werte `Bachelor`, `Master`, `Diplom` oder `Dissertation` hat
- [ ] `./academic_context.md` unterstützt ein Bypass-Flag `humanizer_de: off`, das den Schritt vollständig überspringt
- [ ] `commands/setup.md` prüft beim Setup, ob der `humanizer-de`-Skill unter `~/.codex/skills/humanizer-de/` vorhanden ist; fehlt er, gibt Setup einen Hinweis aus (kein Hard-Fail)
- [ ] In `tests/evals/` existiert ein Eval-Set `evals/humanizer-de-pipeline/` mit mindestens 3 Drafts inkl. GPTZero-Score-Vergleich vor/nach dem Humanizer-Pass
- [ ] Ist `output_target` nicht gesetzt oder kein Hochschul-Marker enthalten (d. h. kein `onboard-project` mit Hochschulkontext), bleibt der Humanizer-Schritt standardmäßig deaktiviert
- [ ] `agents/quality-reviewer.md` erhält einen Hinweis, dass der `humanizer-de(audit)`-Pass bereits vor dem Reviewer gelaufen ist (kein doppelter Audit-Lauf)

---

## Out of Scope

- `style-evaluator` (Roadmap §7.2 Punkt 1) bleibt in diesem Ticket unverändert — separater Folge-Task.

---

## Notes / Links

- Audit-Roadmap §7: [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §7
- Upstream-Referenz humanizer-de: https://github.com/marmbiz/humanizer-de#
- Schließt (TEMP): #59
- Betroffene Code-Pfade (Wave 2):
  - `skills/chapter-writer/SKILL.md`
  - `agents/quality-reviewer.md`
  - `commands/setup.md`
- Skill ist bereits global vendoriert (`~/.codex/skills/humanizer-de/`), keine Re-Implementierung nötig (vgl. Roadmap §7.1)
- Schutz-Ziel: Turnitin / GPTZero / OriginalityAI (vgl. Roadmap §7.4)
