**Audit-Bezug:** [`docs/AUDIT-v6-vault.md`](../blob/main/docs/AUDIT-v6-vault.md) §5 Phase 2

**Blocked by:** Vault Phase 1

## Scope
- [ ] `agents/quote-extractor.md` schreibt Output via `vault.add_quote()` (mit `api_response_id`)
- [ ] `skills/citation-extraction/SKILL.md` liest via `vault.find_quotes()` + `vault.get_quote()`
- [ ] `skills/chapter-writer/SKILL.md` liest via `vault.search()` + `vault.find_quotes()` statt full `literature_state.md`
- [ ] PDFs via `vault.ensure_file()` → `file_id` statt base64
- [ ] `literature_state.md` wird nur noch als read-only Snapshot-Export aus dem Vault generiert

## Akzeptanzkriterien
- [ ] `chapter-writer` < 2k Token Boilerplate (vorher ~10k)
- [ ] Eval: identische Zitat-Qualität bei -75 % Tokens
