# v6.0 Wave 1 Context

Generated: 2026-05-08 by `/mmp:run v6.0` Phase 0.

## Tickets (10 open, milestone v6.0)

### #55 — Baseline-Eval-Run v5.2.0 durchführen und Report ergänzen
labels: documentation, enhancement, v6, evals
URL: https://github.com/jamski105/academic-research/issues/55
**Ziel:** Erstmaliger Eval-Lauf gegen v5.2.0-Baseline; Report `docs/evals/2026-04-23-summary.md`
"Baseline-Gap"-Block füllen (Quality-Gap ≥ 20pp, Trigger-Recall ≥ 85%, Trigger-FPR ≤ 10%).
**Anmerkung:** Braucht `ANTHROPIC_API_KEY` — interaktiver Eval-Lauf.

### #62 — Vault Phase 1 — academic-vault MCP-Server (SQLite + FTS5 + sqlite-vec + Files-API-Cache)
labels: enhancement, v6, vault
URL: https://github.com/jamski105/academic-research/issues/62
**Ziel:** Eigener MCP-Server `academic-vault` als Single Source of Truth.
Schema: `papers, quotes, quote_embeddings, decisions, notes, papers_fts`.
MCP-Tools: `vault.search/get_paper/add_paper/ensure_file/add_quote/find_quotes/get_quote/stats`.
`vault.add_quote` lehnt Quotes ohne `api_response_id` ab (Halluzinationsschutz).
Audit: `docs/AUDIT-v6-vault.md` §3 Option C.

### #63 — Vault Phase 2 — quote-extractor & chapter-writer auf Vault umstellen
labels: refactor, v6, vault
URL: https://github.com/jamski105/academic-research/issues/63
**Blocked by:** #62.
**Ziel:** `quote-extractor` schreibt via `vault.add_quote()`, `chapter-writer` liest via
`vault.search()`/`find_quotes()` statt full `literature_state.md`. PDFs via `vault.ensure_file()`.
**AC:** `chapter-writer` < 2k Token Boilerplate (vorher ~10k); identische Zitat-Qualität bei -75% Tokens.

### #64 — Vault Hook — PreToolUse Verbatim-Validation gegen Halluzinationen
labels: security, v6, vault
URL: https://github.com/jamski105/academic-research/issues/64
**Blocked by:** #62, #63.
**Ziel:** `hooks/verbatim-guard.mjs` (PreToolUse für Write `kapitel/*.md`, `*.tex`).
Parser für Anführungszeichen-Spans; Lookup gegen `vault.search_quote_text`; bei mismatch blockieren.
**AC:** Echte Quotes 100% pass, erfundene 100% block, FPR < 5%.

### #65 — F1.1 Files-API für PDFs in quote-extractor (statt base64-Re-Upload)
labels: refactor, v6, tokens
URL: https://github.com/jamski105/academic-research/issues/65
**Ziel:** `quote-extractor` nutzt `source.type: "file"` mit `file_id`, beta-Header
`anthropic-beta: files-api-2025-04-14`. anthropic SDK ≥ 0.40.
**AC:** -75% Input-Tokens über 5 PDFs × 3 Iterationen vs. base64; graceful Fallback.

### #66 — F1.2 1h-TTL-Cache überall (cache_control: ephemeral, ttl=1h)
labels: v6, tokens
URL: https://github.com/jamski105/academic-research/issues/66
**Ziel:** `relevance-scorer`, `quote-extractor`, `quality-reviewer` mit
`cache_control: {type: "ephemeral", ttl: "1h"}`. Breakpoint VOR `documents[]`.
**AC:** Folgecall innerhalb 1h: `cache_read_input_tokens > 0`.
**Hintergrund:** Anthropic änderte 2026-03-06 Default-TTL von 1h auf 5min.

### #67 — F1.3 Skills schlanker: Shared preamble extrahieren
labels: refactor, v6, tokens
URL: https://github.com/jamski105/academic-research/issues/67
**Ziel:** `skills/_common/preamble.md` mit Pflicht-Blöcken; Include in allen 13 Skills;
lazy `references/<variant>.md`; Browser-Modul: nur Schema in Modell-Context.
**AC:** -500 Token pro Skill bei Aktivierung; `tests/test_skills_manifest.py` grün.

### #68 — F4 humanizer-de Integration in chapter-writer + quality-reviewer
labels: enhancement, v6, humanizer
URL: https://github.com/jamski105/academic-research/issues/68
**Ziel:** `chapter-writer` ruft `humanizer-de` im Mode `audit` vor `quality-reviewer`.
Trigger: `output_target ∈ {Bachelor, Master, Diplom, Dissertation}` aus `./academic_context.md`.
Bypass: `humanizer_de: off`.
**AC:** GPTZero-Score-Vergleich vor/nach an 3 Drafts; default off ohne Hochschul-Marker.
**Closes (TEMP):** #59.

### #69 — F4 /academic-research:humanize Slash-Command (eigenständiger Pass)
labels: enhancement, v6, humanizer
URL: https://github.com/jamski105/academic-research/issues/69
**Ziel:** `commands/humanize.md` mit `Skill(humanizer-de)`-Frontmatter.
Argument: `<kapitel-pfad> [--mode normal|deep]`. Output: `<basename>.humanized.md`
+ `<basename>.diff.md` (Severity-gegliedert). Voice-Calibration via
`~/.academic-research/projects/<slug>/voice-samples/`.

### #70 — F12 git init Auto-Setup im /academic-research:setup
labels: enhancement, v6
URL: https://github.com/jamski105/academic-research/issues/70
**Ziel:** `scripts/project_bootstrap.py` fragt "Git aktivieren?" (y/n).
Bei y: `git init` + initial commit. Stop-Hook warnt bei `academic_context.md`-Diff.
`.gitignore`-Erweiterung: `pdfs/, sessions/, credentials.json`.
**AC:** Setup in leerem Ordner → git-Repo nach y; ohne git → graceful Skip.

## Dependency-Graph

```
#62 (Vault Phase 1)  ─┬─►  #63 (Vault Phase 2)  ─►  #64 (Verbatim-Hook)
                       └─►  (#65 nutzt files-api unabhängig)
#65, #66, #67  → unabhängige Token-Tracks
#68 → blocks-on-skill-existenz humanizer-de
#69 → unabhängig
#70 → unabhängig
#55 → Eval-Run, kann jederzeit laufen (Doku)
```

## In-repo docs

- `docs/AUDIT-v6-roadmap.md` (956 Zeilen) — Master-Audit + Roadmap aller F-Felder F1–F16
- `docs/AUDIT-v6-vault.md` (343 Zeilen) — Anhang zum v6-Audit, Vault-Architektur (§F7–F9)
- `README.md` (803 Zeilen) — Plugin-Doku v5.4
- `CHANGELOG.md` — Release-Historie

**Hinweis Konfig vs. Realität:** `config.yaml` listet `docs/milestones/v6.0.md`
und `docs/v6.0.md` als doc-Pfade — beide existieren NICHT. Die tatsächlichen
v6-Dossiers liegen unter `docs/AUDIT-v6-roadmap.md` und `docs/AUDIT-v6-vault.md`
(beide sind im aktuellen Working-Tree noch UNTRACKED — siehe `git status`).
Empfehlung vor Phase 1: AUDIT-Files committen, damit L1-Worktrees sie sehen.

## Linked-issue context

Keine Cross-Links über v6-Tickets hinaus außer #59 (TEMP-close von #68).
