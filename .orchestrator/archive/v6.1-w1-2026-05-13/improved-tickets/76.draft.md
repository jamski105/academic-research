## Context

Ticket #76 gehört zum v6.1-Sprint (§15 der Roadmap). Die bestehenden 16 Eval-Verzeichnisse decken ausschließlich Paper-Pfade ab; Buch-Cases fehlen vollständig. Dieses Ticket legt die Eval-Fixtures und Token-Regression-Baseline an, die alle v6.1 F2.x-Komponenten (#71 `book-handler`, #72 `chunk_pdf`, #73 `page_offset`, #74 OCR, #75 CSL) testbar machen. Außerdem werden die bestehenden Evals um `tokens_in`/`tokens_out`-Felder erweitert, sodass die bereits gemergten v6.0 F1.1/F1.2-Baseline-Werte (commits c7161aa, #119) als Regressionsgrenze dienen.

## Acceptance Criteria

- [ ] `evals/book-handler/evals.json` existiert mit genau 5 Cases: 1 OA-Buch (DOAB), 2 ISBN-only ohne PDF, 1 Scan-PDF (für OCR-Pfad), 1 Sammelband mit mindestens 3 Editor-Kapiteln
- [ ] `evals/humanizer-de-pipeline/` enthält 3 Drafts jeweils als Vorher/Nachher-Paar (6 Dateien oder äquivalente Struktur)
- [ ] Alle bestehenden `evals/*/evals.json`-Dateien enthalten die Felder `tokens_in` und `tokens_out` pro Case
- [ ] Regression-Test schlägt fehl, wenn `tokens_in` oder `tokens_out` gegenüber der gespeicherten Baseline um mehr als 20 % steigen
- [ ] CI-Lauf (lokales `pytest tests/`) ist grün nach diesen Änderungen

## Notes / Links

- Roadmap-Quelle: [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §15
- Bestehende Baseline-Dateien: `docs/evals/2026-04-23-summary.md` und `tests/baselines/`
- Abhängigkeiten: #71 `book-handler`, #72 `chunk_pdf`, #73 `page_offset`, #74 OCR, #75 CSL (v6.1 F2.x)
- F1.1/F1.2 bereits gemergt: c7161aa (humanizer-de Integration), #119 (Vault-Interface-Migration)

## Open Questions

- Sollen die Fixtures (PDFs / Metadaten-Dateien) der 5 Buch-Cases direkt ins Repo eingecheckt werden, oder genügt eine Referenz auf einen bekannten externen Speicherort (z. B. DOAB-URL + ISBN)? Die Roadmap nennt beide Optionen ohne Entscheidung.
- Auf welche konkrete Baseline-Messung (Datei + Commit) soll sich der +20 %-Schwellenwert für `tokens_in`/`tokens_out` beziehen — `docs/evals/2026-04-23-summary.md`, `tests/baselines/` oder ein neu zu erstellender Snapshot?
