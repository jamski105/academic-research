## Context

Bibliotheken verlangen vorbereitete Listen für Vorbestellung und Fernleihe. Aktuell entstehen ad-hoc-Lösungen pro Recherche; `/academic-research:pickup` standardisiert diesen Schritt als wiederverwendbaren Slash-Command.

**Einordnung v6.1 → v6.2:** Die v6.1-Buch-Pipeline (`book-handler`, `chunk_pdf`, `page_offset`, OCR-Trigger, CSL-Schema) liefert die Grundlage: Signatur und Standort aus dem OPAC-Lookup sowie ISBN-Normalisierung sind bereits im `book-handler`-Output vorhanden und können direkt als Input für die Pickup-Liste genutzt werden.

**Audit-Bezug:** [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §8.2 + §18.3

## Acceptance Criteria

- [ ] `/academic-research:pickup` ist als Slash-Command aufrufbar und akzeptiert eine Vault-Auswahl als Input
- [ ] Das erzeugte `pickup-list.xlsx` enthält 4 Sheets: `Books` (Titel, Autor, ISBN, Code128-Barcode, Signatur, Standort), `Articles` (DOI, Journal, Volume, Issue, Pages), `Theses` (Autor, Hochschule, Jahr, URL), `Online` (URL, Zugriffsdatum, Backup-Status)
- [ ] Code128-Barcodes für die ISBN-Spalte im `Books`-Sheet sind als Bild in der jeweiligen Zeile eingebettet
- [ ] Excel-Generierung erfolgt ausschließlich über den `document-skills:xlsx`-Skill (kein openpyxl, pandas oder Custom-Skript)
- [ ] Ein Test mit 5 Beispielquellen (min. je 1 Buch, 1 Artikel, 1 These, 1 Online-Quelle) erzeugt eine valide `.xlsx`-Datei ohne Fehler
- [ ] Die Barcodes der 5 Testquellen sind mit einem handelsüblichen Barcode-Scanner oder einer Smartphone-App scanbar (visuelle Verifikation genügt)

## Notes / Links

- Audit-Roadmap: [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §8.2 + §18.3
- Repo-Konvention: Excel via `document-skills:xlsx`-Skill (User-Memory `feedback_excel_skill.md`) — kein openpyxl/pandas
- Spec-Ablage: `specs/v6.2/F5.md`
- Foundation: v6.1-Buch-Pipeline auf `main` (book-handler, OPAC-Signatur/Standort, ISBN-Normalisierung)

## Open Questions

- **Sheet-Struktur:** Der Ticket-Body definiert 4 Sheets nach **Quellentyp** (`Books`, `Articles`, `Theses`, `Online`). Audit-Roadmap §8.2 spezifiziert 4 Sheets nach **Verfügbarkeitsstatus** (`Vor Ort verfügbar`, `Fernleihe`, `Online OA`, `Lizenz nötig`). Welches Schema gilt für v6.2?
- **Barcode-Einbettung:** Sollen Code128-Barcodes als Zellbild in Excel eingebettet werden, oder genügt die codierte Zeichenkette (z. B. via Barcode-Schriftart)? Relevant für Umsetzbarkeit im `document-skills:xlsx`-Skill.
- **Input-Scope:** Soll `/academic-research:pickup` ausschließlich auf Einträge mit `source != open_access` filtern (wie §8.2 impliziert), oder alle Vault-Einträge der Auswahl unabhängig vom OA-Status aufnehmen?
