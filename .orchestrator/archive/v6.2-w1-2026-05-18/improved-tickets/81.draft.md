## Context

Vier OA-Site-Subagenten (`tib-fetcher`, `oapen-fetcher`, `doabooks-fetcher`, `kvk-fetcher`) sind der erste Sprint-Deliverable von F16 (Universal Book Fetcher) in v6.2. Sie bilden den Open-Access-Kern der browser-use-only Subagenten-Architektur aus `docs/AUDIT-v6-roadmap.md` §15a F16.1: Jeder Agent kennt nur eine Site, navigiert sie wie ein Mensch per `browser-use`, und liefert entweder einen lokalen PDF-Pfad oder einen strukturierten `metadata_only`-Status zurück.

Fundament: Die v6.1-Buch-Pipeline (`book-handler`, `chunk_pdf`, `page_offset`, OCR-Detection, CSL-Schema mit `type: book|chapter`) ist auf `main` verfügbar und setzt den Kontrakt für das Output-Schema voraus (#80). Jeder Subagent referenziert den jeweiligen Browser-Guide (#87). Ticket #81 beschränkt sich auf die vier Auth-freien OA-Quellen; Auth-pflichtige Subagenten (springer-book-fetcher, degruyter-fetcher, ebook-central-fetcher, nationallizenzen-fetcher) und der Master-Agent `book-fetcher` sind separater Scope.

**Audit-Bezug:** [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §15a F16.1

## Acceptance Criteria

**tib-fetcher (`agents/tib-fetcher.md`)**
- [ ] Agent-File vorhanden mit `model: sonnet`, `tools: [Bash(browser-use:*), Read, Write]`, `maxTurns: 15`
- [ ] Eingabe-Trigger: ISBN, DOI oder Freitext-Titel
- [ ] Discovery-Schritte: TIB-Suche (`tib.eu/de/suchen?query=…`) → Treffer-Auswahl anhand Titel/Autor/Jahr-Match → Detailseite
- [ ] OA-Filter-Logik: Agent prüft auf "Open Access"-Badge und "Volltext"-Block vor Download-Versuch; ohne OA-Indiz → `metadata_only`
- [ ] Volltext-Lokation: `browser-use download` auf PDF-Link → Pfad unter `$OUTPUT_PATH`; PDF-Validation (Magic-Bytes, Größe > 10 KB)
- [ ] Fallback-Output: `{"status": "metadata_only", "url": "<detailseite>"}` wenn kein Volltext auffindbar
- [ ] Verbote im System-Prompt dokumentiert: kein curl/wget, keine direkten HTTP-Calls, keine fingierten Treffer
- [ ] Eval-Case: 1 bekanntes OA-Buch aus TIB → Agent liefert lokalen PDF-Pfad

**oapen-fetcher (`agents/oapen-fetcher.md`)**
- [ ] Agent-File vorhanden mit `model: sonnet`, browser-use-Tool-Restriction, `maxTurns` gesetzt
- [ ] Eingabe-Trigger: ISBN, DOI oder Titel
- [ ] Discovery-Schritte: `oapen.org`-Suche → Treffer-Selektion → Detailseite (kein Auth erforderlich, OA-Plattform)
- [ ] OA-Filter-Logik: da oapen.org ausschließlich OA-Bücher hostet, ist jeder Treffer per Definition OA; Agent verweist auf diese Invariante
- [ ] Volltext-Lokation: Download-Link auf Detailseite identifizieren und per `browser-use download` sichern
- [ ] Fallback-Output: strukturiertes `metadata_only`-JSON wenn kein Download-Link gefunden
- [ ] Eval-Case: 1 bekanntes OAPEN-Buch → Agent liefert lokalen PDF-Pfad

**doabooks-fetcher (`agents/doabooks-fetcher.md`)**
- [ ] Agent-File vorhanden mit `model: sonnet`, browser-use-Tool-Restriction, `maxTurns` gesetzt
- [ ] Eingabe-Trigger: ISBN, DOI oder Titel
- [ ] Discovery-Schritte: `directory.doabooks.org`-Suche via Browser (REST-Endpunkte werden über Browser bedient, kein direkter API-Call) → Treffer-Liste → Detailseite
- [ ] OA-Filter-Logik: DOAB listet ausschließlich OA-Bücher; Agent prüft trotzdem auf verfügbaren Volltext-Link (nicht alle DOAB-Einträge haben direkt hinterlegten PDF-Download)
- [ ] Volltext-Lokation: Externer Verlags-Link oder Repository-Link auf Detailseite → Browser-Navigation zu Download
- [ ] Fallback-Output: strukturiertes `metadata_only`-JSON mit Detailseiten-URL
- [ ] Eval-Case: 1 bekanntes DOAB-Buch → Agent liefert lokalen PDF-Pfad oder begründetes `metadata_only`

**kvk-fetcher (`agents/kvk-fetcher.md`)**
- [ ] Agent-File vorhanden mit `model: sonnet`, browser-use-Tool-Restriction, `maxTurns` gesetzt
- [ ] Eingabe-Trigger: ISBN, DOI oder Titel
- [ ] Discovery-Schritte: `kvk.bibliothek.kit.edu`-Suche → Ergebnisliste aus 80+ Katalogen lesen → OA-Treffer identifizieren (Volltext-Links in Trefferliste oder Detailseite)
- [ ] OA-Filter-Logik: KVK ist Meta-Katalog (nicht ausschließlich OA); Agent filtert Treffer auf solche mit Volltext-Link oder OA-Indikator; ohne OA-Indiz → `metadata_only` mit Bibliotheks-Standorten für Pickup-Pfad
- [ ] Volltext-Lokation: Bei Volltext-Link → weiternavigieren zum Download; bei reinem Bibliotheks-Nachweis → `metadata_only` mit Standort-Info
- [ ] Eval-Case: 1 Buch (OA oder Nicht-OA) → korrekte Unterscheidung zwischen Volltext-Erfolg und `metadata_only`+Standort

**Übergreifend**
- [ ] Alle 4 Agent-Files referenzieren ihren jeweiligen Browser-Guide aus `config/browser_guides/` (#87)
- [ ] Output-Schema aller 4 Agenten ist gemäß Master-Schema (#80) einheitlich
- [ ] Kein Agent enthält direkten HTTP-/API-Call-Code; `browser-use`-only ist im System-Prompt als Verbot verankert

## Out of Scope

- Master-Agent `book-fetcher` und `/academic-research:fetch`-Command (F16.2/F16.3)
- Auth-pflichtige Subagenten (springer-book-fetcher, degruyter-fetcher, ebook-central-fetcher, nationallizenzen-fetcher, auth-helper)
- Per-Uni-Profile und HAN/Shibboleth/EZproxy-Flows (F16.5)
- `generic-fetcher` Discovery-Modus (F16.4)
- Neue `config/browser_guides/`-Dateien für diese Sites (sofern in #87 abgedeckt)

## Notes / Links

- Audit-Referenz: [`docs/AUDIT-v6-roadmap.md`](../blob/main/docs/AUDIT-v6-roadmap.md) §15a F16.1
- Architektur-Skelett für `tib-fetcher.md` inkl. Standard-Flow und Verboten: §15a F16.1 (Roadmap-Datei, ab Zeile 562)
- Master Output-Schema (JSON mit `status`, `pdf_path`, `source_subagent`, `tries`): Roadmap §15a F16.2
- Abhängigkeit: Master-Schema-Ticket #80, Browser-Guide-Ticket #87
- v6.1-Foundation (book-handler, chunk_pdf, page_offset, OCR-Detection, CSL book/chapter): auf `main` verfügbar seit Commits `d12a976`, `8a7a2ad`, `eaaa412`

## Open Questions

- Welches Test-Buch (Titel + ISBN) soll für jeden der 4 Eval-Cases als Fixture gelten — soll das im Ticket fixiert werden oder überlässt der Autor das dem Implementierer?
- `kvk-fetcher`: Wenn ein KVK-Treffer `metadata_only`+Standort liefert, soll der Agent diese Standort-Information direkt in `pickup-list.xlsx` schreiben oder nur im Output-JSON zurückgeben (damit der Master-Agent entscheidet)?
